{
  "name": "angular-gnavi",
  "private": true,
  "version": "0.0.0",
  "description": "A gnavi project for AngularJS",
  "repository": "https://github.com/komushi/angular-gnavi",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.6.1",
    "bower": "^1.3.1"
  },
  "scripts": {
    "postinstall": "bower install --allow-root",

    "prestart": "npm install",
    "start": "http-server ./app -p 9000 -d false -i true",


    "pretest": "npm install",

    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update"
    
  }
}
